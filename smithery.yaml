startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    description: Cerebral Valley enables AI assistants to search, discover, and submit AI community events and hackathons.
  commandFunction: |
    (config) => ({
      command: 'bun',
      args: ['run', 'src/server.ts']
    })
  exampleConfig: {}
